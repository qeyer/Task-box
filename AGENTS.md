# 全局工作约定

本文件可合并到用户级 `AGENTS.md`，只提供跨项目通用的任务收纳规则。项目自己的目录结构和说明优先。

## 任务收纳

- 用户授权执行后，自行判断是否使用 `$task-box`，不另行询问是否使用技能；讨论建议阶段不创建或整理任务目录。
- 新项目、新支线、预计会产生多个文件的独立任务，或用户明确要求整理文件时，在第一次创建文件前建立独立任务目录。
- 纯问答、只读分析、状态查看和明确输出位置的单文件小修改，不创建任务目录。
- 当前目录或已有任务目录中存在 `工作文件/任务记录.md` 时，先按对象、目标、素材和交付物判断是否续做；旧任务已有根目录 `任务说明.md` 时继续复用。
- 新任务默认使用 `YYYY-MM-DD_对象_具体任务` 命名；同名但不同任务增加区分词或递增后缀，不覆盖或静默合并。
- 一级目录统一使用 `素材`、`工作文件`、`预览`、`成品`、`归档`，只创建实际需要的目录。领域差异放在二级目录。
- 一次性简单任务不强制生成记录；需要续做、交接、待办或多轮版本管理时，在 `工作文件/任务记录.md` 记录目标、状态、当前成品链接和下一步。
- 用户指定路径、目录名或结构时优先服从；未经授权，不移动、重命名、覆盖或删除既有文件。
- 在代码仓库中保持源码、测试和配置的原有结构；任务资料、截图、验证记录和导出物才放入任务目录。
