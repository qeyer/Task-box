# 全局工作约定

## 任务收纳

- 每次收到新请求时，先判断它是否属于新项目、新支线或会产生多个文件的新任务。
- 如果属于上述情况，在第一次创建文件之前自动使用 `$task-box`，在当前项目根目录内建立独立任务目录，并把后续新增的素材副本、脚本、清单、预览和交付物放入对应位置。
- 如果当前目录或已有任务目录中存在 `任务说明.md`，优先判断为继续原任务并复用，不重复新建。
- 纯问答、只读分析、状态查看，以及输出位置已经明确的单文件小修改，不创建新的任务目录。
- 用户明确指定路径、文件夹名称或目录结构时，优先服从用户要求。
- 不因自动收纳而擅自移动、重命名、覆盖或删除用户已有文件；整理既有文件必须获得明确授权。
- 在已有代码仓库中，不移动源代码或破坏仓库结构；仅按 `$task-box` 的规则整理需求、计划、截图、验证记录和导出物。
- 自动收纳应在任务开始写文件前完成，不要等任务结束后再批量搬运。
